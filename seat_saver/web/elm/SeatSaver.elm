module SeatSaver where

-- Imports HTML so that we can use its functions.
import Html

-- Every Elm application must have a main function that acts as its starting point.
main =
    -- Will result in the string being written out to the screen.
    Html.text "Hello from Elm"